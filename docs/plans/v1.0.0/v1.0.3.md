# v1.0.3 Bio 結構重構與 AI 助手標註

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 將 `ResumeData` 的 Bio 管理從單一字串拆分為結構化欄位，並正式標註 AI 助手功能的 Mock 狀態以避免冗餘檢查。

**Architecture:** 
1. 重構 `ResumeData` 模型，移除 `bio` 並新增 `introBio`, `experienceBio`, `projectBio`。
2. 更新 `constants.dart` 內的對應內容。
3. 修改 `HeroSection` 直接讀取新欄位渲染。
4. 在文件與程式碼中標註 AI 助手功能尚未規劃，以提供給未來 Agent 參考。

**Tech Stack:** Flutter, models, architecture refinement

---

## Task 1：重構 ResumeData 模型

**Files:**
- Modify: `lib/models/resume_model.dart`

**Step 1：修改欄位定義**
- 移除 `final String bio`。
- 新增 `final String introBio`, `final String experienceBio`, `final String projectBio`。
- 更新建構函式。

---

## Task 2：資料遷移與常數更新

**Files:**
- Modify: `lib/utils/constants.dart`

**Step 2：遷移內容**
- 將 `resumeDataZh`, `resumeDataEn`, `resumeDataJa` 內原本用 `\n\n` 拆分的內容填入新欄位：
    - 第一段 (關於我/教育) -> `introBio`
    - 第二段 (工作經歷摘要) -> `experienceBio`
    - 第三段 (個人專案摘要) -> `projectBio`

---

## Task 3：HeroSection UI 組裝重構

**Files:**
- Modify: `lib/ui/widgets/sections/hero_section.dart`

**Step 3：移除字串拆分邏輯**
- 移除 `resumeData.bio.split('\n\n')` 字串操作。
- 修改 `_buildBioContent` 改為直接接收這三個獨立欄位並依序渲染。

---

## Task 4：專案文件與代碼標註 (已預先完成)

**Files:**
- Modify: `docs/decisions/project-overview.md` (已更新)
- Modify: `lib/ui/widgets/ai_assistant_widget.dart` (已更新)

**決策紀錄：**
- 明確標註 `AiAssistantWidget` 僅為 Mock，不具實質功能。
- 確認目前無需實作 `fullBio` (合併回傳) 功能。

---

## 驗證與測試

**Step 4：靜態分析與單元測試**

```bash
flutter analyze
flutter test
```

預期：
```
No issues found! (除已知的 deprecated 外)
All tests passed!
```

---

## Commit 紀錄

```bash
git add lib/models/resume_model.dart lib/utils/constants.dart lib/ui/widgets/sections/hero_section.dart docs/decisions/project-overview.md lib/ui/widgets/ai_assistant_widget.dart docs/plans/v1.0.0/v1.0.3.md
git commit -m "refactor: restructure resume bio into intro, experience, and project fields"
```
