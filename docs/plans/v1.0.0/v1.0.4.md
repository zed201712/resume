# v1.0.4 同步多語言專案描述

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 依據 `resumeDataZh` 的最新專案內容，同步更新 `resumeDataEn` 與 `resumeDataJa` 的專案資訊，確保多語言內容的一致性與條列式格式。

**Architecture:** 
1. 更新 `lib/utils/constants.dart` 中的 `resumeDataEn` 物件。
2. 更新 `lib/utils/constants.dart` 中的 `resumeDataJa` 物件。

**Tech Stack:** Flutter, I18n, Documentation

---

## Task 1：更新英文版專案描述 (resumeDataEn)

**Files:**
- Modify: `lib/utils/constants.dart`

**Step 1：更新 `resumeDataEn` 的 `projects` 欄位**
- 將 `title` 與 `description` 更新為與中文版對應的在地化內容。
- 採用條列式格式。

---

## Task 2：更新日文版專案描述 (resumeDataJa)

**Files:**
- Modify: `lib/utils/constants.dart`

**Step 2：更新 `resumeDataJa` 的 `projects` 欄位**
- 將 `title` 與 `description` 更新為與中文版對應的在地化內容。
- 採用條列式格式。

---

## 驗證與測試

**Step 3：靜態分析**

```bash
flutter analyze
```

預期：
```
No issues found!
```

---

## Commit 紀錄

```bash
git add lib/utils/constants.dart docs/plans/v1.0.0/v1.0.4.md
git commit -m "docs: plan v1.0.4 and sync project descriptions for En and Ja"
```
